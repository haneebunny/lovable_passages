// "use client";

// import { SessionProvider, useSession } from "next-auth/react";

// function Wrapping() {

//   return <SessionProvider>{children}</SessionProvider>;

// }

// export default function UserSetting({ children }) {
//   const { data } = useSession();

//   return <SessionProvider>{children}</SessionProvider>;
// }
